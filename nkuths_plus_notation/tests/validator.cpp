#include <bits/stdc++.h>
#include "constraints.hpp"
#include "testlib.h"
using namespace std;
using ull = unsigned long long;

int main(int argc, char* argv[]) {
	registerValidation(argc, argv);
	ll N = inf.readLong(ull(MIN_N), ull(MAX_N), "N");
	inf.readSpace();
	ll A = inf.readLong(ull(MIN_A), ull(MAX_A), "A");
	inf.readSpace();
	ll B = inf.readLong(ull(MIN_B), ull(MAX_B), "B");
	inf.readEoln();
	inf.readEof();
	return 0;
}
